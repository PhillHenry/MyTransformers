[project]
name = "mytransformers"
version = "0.1.0"
description = "Katas for Python Transformers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "numpy",
    "sentencepiece",
    "protobuf",
]

[tool.uv]
package = false